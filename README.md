# normalize-global-css

Combined CSS reset including:

- normalize.css v8.0.1
- Global style reset rules
- Cross-browser fixes

## Installation

```bash
npm install w-normalize-global-css
yarn install w-normalize-global-css
pnpm install w-normalize-global-css
```
